#ifndef PIPELINE_H
#define PIPELINE_H

// Gére l'exécution des commandes avec des pipes
int handle_pipe(char **tokens);

// Vérifie la présence d'un pipe dans les tokens
int check_pipe(char **tokens);

#endif
